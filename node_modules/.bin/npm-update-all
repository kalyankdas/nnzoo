#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-update-all/npm-update-all.js" "$@"
  ret=$?
else 
  node  "$basedir/../npm-update-all/npm-update-all.js" "$@"
  ret=$?
fi
exit $ret
